# Live Server 热重载开发指南

## 🔥 什么是热重载？

热重载（Hot Reload）是一种开发技术，当你修改代码文件时，浏览器会自动刷新，无需手动刷新页面。这大大提高了开发效率。

## 🚀 快速开始

### 启动服务器

```bash
./start-server.sh
```

启动后会显示：
```
✅ Live Server 启动成功！

📱 访问地址：
   - 本地: http://localhost:8000/
   - 测试页面: http://localhost:8000/test.html

🔥 热重载已启用，修改文件后浏览器会自动刷新
```

### 查看状态

```bash
./status-server.sh
```

显示服务器运行状态、进程信息、端口监听等。

### 停止服务器

```bash
./stop-server.sh
```

## 📝 使用方法

### 1. 启动服务器

```bash
./start-server.sh
```

### 2. 在浏览器中打开应用

访问：http://localhost:8000/

### 3. 修改代码

修改 `www/` 目录下的任何文件：
- `www/index.html` - 修改页面结构
- `www/css/index.css` - 修改样式
- `www/js/index.js` - 修改逻辑
- `www/js/storage.js` - 修改存储模块

### 4. 自动刷新

保存文件后，浏览器会自动刷新，立即看到效果！

## 🎯 实际示例

### 示例 1：修改标题颜色

1. 打开 `www/css/index.css`
2. 找到 `header h1` 样式
3. 修改颜色：
   ```css
   header h1 {
       font-size: 28px;
       color: #ff6b6b;  /* 改成红色 */
       margin-bottom: 15px;
   }
   ```
4. 保存文件
5. 浏览器自动刷新，标题变成红色！

### 示例 2：修改按钮文字

1. 打开 `www/index.html`
2. 找到打卡按钮：
   ```html
   <div class="btn-text">打卡喝水</div>
   ```
3. 修改为：
   ```html
   <div class="btn-text">喝水啦</div>
   ```
4. 保存文件
5. 浏览器自动刷新，按钮文字改变！

### 示例 3：添加新功能

1. 打开 `www/js/index.js`
2. 在 `handleCheckin` 函数中添加提示：
   ```javascript
   handleCheckin: function() {
       const now = new Date();
       const record = {
           id: now.getTime(),
           time: this.formatTime(now)
       };

       if (Storage.saveRecord(record)) {
           this.updateUI();
           this.showFeedback();
           alert('打卡成功！继续保持！'); // 新增
       }
   },
   ```
3. 保存文件
4. 浏览器自动刷新
5. 点击打卡按钮，会弹出提示！

## 📊 监控日志

### 实时查看日志

```bash
tail -f live-server.log
```

### 查看最近日志

```bash
tail -20 live-server.log
```

### 日志内容示例

```
Serving "www" at http://127.0.0.1:8000
Ready for changes
Change detected www/css/index.css
Change detected www/js/index.js
```

## 🛠️ 高级用法

### 后台运行

服务器使用 `nohup` 在后台运行，即使关闭终端也会继续运行。

### 查看进程

```bash
ps aux | grep live-server
```

### 手动停止进程

```bash
pkill -f "live-server.*www"
```

### 更改端口

编辑 `start-server.sh`，修改端口号：
```bash
nohup npx live-server www --port=3000 --host=0.0.0.0 --no-browser > live-server.log 2>&1 &
```

## 🔍 故障排查

### 问题 1：端口被占用

**症状**：启动失败，提示端口 8000 被占用

**解决**：
```bash
# 查看占用端口的进程
lsof -i :8000

# 停止进程
./stop-server.sh

# 或手动停止
kill -9 <PID>
```

### 问题 2：浏览器不自动刷新

**症状**：修改文件后浏览器不刷新

**解决**：
1. 检查服务器是否运行：`./status-server.sh`
2. 查看日志：`tail -f live-server.log`
3. 确保修改的是 `www/` 目录下的文件
4. 尝试手动刷新浏览器（Ctrl+R）

### 问题 3：无法访问

**症状**：浏览器显示无法连接

**解决**：
```bash
# 检查服务器状态
./status-server.sh

# 检查端口监听
netstat -tlnp | grep 8000

# 重启服务器
./stop-server.sh
./start-server.sh
```

## 📋 命令速查表

| 命令 | 说明 |
|------|------|
| `./start-server.sh` | 启动 live-server |
| `./stop-server.sh` | 停止 live-server |
| `./status-server.sh` | 查看服务器状态 |
| `tail -f live-server.log` | 实时查看日志 |
| `ps aux \| grep live-server` | 查看进程 |
| `netstat -tlnp \| grep 8000` | 查看端口 |

## 🎨 开发工作流

### 推荐工作流

1. **启动服务器**
   ```bash
   ./start-server.sh
   ```

2. **打开浏览器**
   - 访问 http://localhost:8000/
   - 打开开发者工具（F12）

3. **开始开发**
   - 修改 HTML/CSS/JS 文件
   - 保存后自动刷新
   - 在开发者工具中查看效果

4. **调试**
   - 使用 Console 查看日志
   - 使用 Elements 检查 DOM
   - 使用 Network 查看请求

5. **完成后停止**
   ```bash
   ./stop-server.sh
   ```

### 多窗口开发

建议打开多个终端窗口：

**窗口 1：服务器日志**
```bash
tail -f live-server.log
```

**窗口 2：代码编辑**
```bash
vim www/js/index.js
# 或使用你喜欢的编辑器
```

**窗口 3：Git 操作**
```bash
git status
git add .
git commit -m "更新功能"
```

## 💡 最佳实践

### 1. 保持服务器运行

开发期间保持 live-server 运行，不要频繁启停。

### 2. 使用开发者工具

- 打开浏览器开发者工具（F12）
- 使用 Console 查看 JavaScript 错误
- 使用 Elements 调试 CSS

### 3. 小步提交

每完成一个小功能就提交一次：
```bash
git add .
git commit -m "添加XXX功能"
```

### 4. 测试数据

使用测试页面 http://localhost:8000/test.html 进行功能测试。

### 5. 清理缓存

如果修改没有生效，尝试：
- 硬刷新：Ctrl+Shift+R
- 清除浏览器缓存
- 重启 live-server

## 🚀 性能优化

### 减少不必要的刷新

live-server 会监听所有文件变化。如果有临时文件或日志文件，可以配置忽略：

创建 `.live-server.json`：
```json
{
  "port": 8000,
  "host": "0.0.0.0",
  "root": "www",
  "ignore": "*.log,*.tmp"
}
```

### 使用浏览器缓存

开发时可以禁用缓存，生产环境启用缓存。

## 📚 相关资源

- [live-server 官方文档](https://www.npmjs.com/package/live-server)
- [Cordova 开发指南](https://cordova.apache.org/docs/en/latest/guide/overview/)
- [Chrome DevTools 文档](https://developer.chrome.com/docs/devtools/)

## ✨ 总结

使用 live-server 进行热重载开发：
- ✅ 自动刷新，提高效率
- ✅ 后台运行，不占用终端
- ✅ 实时日志，方便调试
- ✅ 简单易用，一键启动

开始享受高效的开发体验吧！🎉
